{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "gcc build and debug active file",
            "type": "lldb",
            "request": "launch",
            "program": "build/sdlpipboy.out",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build DEBUG",
        }
    ]
}
